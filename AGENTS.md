# Agent Entry Point

Read `GRAPHIFY.md` to understand the intended project context map and search keywords.

Key constraints:

- Work on Windows from `D:\CODE\VedaAI`.
- Keep frontend and backend in separate folders.
- Use the provided Figma assets in `design\`.
- Use Docker Compose for MongoDB, Redis, backend API, backend worker, and frontend when practical.
- Use Gemini through `@google/genai` if AI generation is implemented.
- Do not render raw LLM responses.
- Keep comments natural and useful.
- Do not add trailing separator lines.

